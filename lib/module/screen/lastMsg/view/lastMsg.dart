import 'package:flutter/material.dart';

class lastMsg extends StatelessWidget {
  const lastMsg({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Image.asset('assets/images/job.png'),
      ),
    );
  }
}
